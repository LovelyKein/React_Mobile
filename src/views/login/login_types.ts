export interface TestCode {
  code: number;
  codeText: string;
}

export interface LoginInfo {
  code: number;
  codeText: string;
  token: string;
}

export interface LoginForm {
  phone: string;
  code: string;
}